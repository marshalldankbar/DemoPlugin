{
  "name": "marshalls-quiz-plugin",
  "version": "1.0.0",
  "description": "A WordPress plugin to engage visitors with a Developer Personality Quiz.",
  "main": "index.js",
  "scripts": {
    "build": "wp-scripts build",
    "start": "wp-scripts start",
    "sync": "browser-sync start --proxy 'yourlocal.dev' --files 'build/*.css, build/*.js'",
    "preview": "npm-run-all --parallel sync start"
  },
  "author": "Marshall Dankbar",
  "license": "GPL-2.0-or-later",
  "devDependencies": {
    "@wordpress/scripts": "^19.1.0",
    "browser-sync": "^2.27.7",
    "npm-run-all": "^4.1.5"
  }
}
